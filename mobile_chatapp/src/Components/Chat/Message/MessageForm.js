import React from 'react';
import {
  StyleSheet,
  View,
} from 'react-native';

const MessageForm: () => React$Node = () => {
  return (
    <View style={ styles.main }>

    </View>
  );
};

const styles = StyleSheet.create({
  main: {
    padding: 15
  }
})

export default MessageForm;
